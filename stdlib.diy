;; Some logical operators.

(define not
    (lambda (b)
        (if b #f #t)))

(define or
    (lambda (a b)
        (if (eq #t a) #t
        (if (eq #t b) #t
             #f))))


;; DIY -- Implement the rest of your standard library
;; here as part 7 of the workshop.
